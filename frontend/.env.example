# Optional: full browser-facing API base, including the /api/v1 prefix.
# NEXT_PUBLIC_API_URL=https://api.example.com/api/v1

# Optional: backend origin or API base used by Next rewrites.
# Local development defaults to http://127.0.0.1:8000 only.
# BACKEND_API_URL=https://api.example.com
